# {{ project_name }}

> 由 `agent-cli init` 生成（模板: agentscope）

## 快速开始

```bash
# 安装依赖
uv sync

# 运行 Agent
uv run python -m agent.main "你好"

# 运行测试
uv run pytest
```

## 项目结构

```
{{ project_name }}/
├── src/agent/
│   ├── __init__.py
│   ├── main.py          # Agent 入口
│   └── tools/           # 自定义工具
├── tests/
│   └── test_main.py
├── .env.example
├── pyproject.toml
└── agent-cli-manifest.yaml
```

## 下一步

1. 在 Coding Agent 中打开此目录
2. 告诉它你的 Agent 想做什么
3. 让它根据 workflow Skill 引导你完成设计和实现

---

*Generated by agent-cli-alicloud v{{ cli_version }} at {{ created_at }}*
