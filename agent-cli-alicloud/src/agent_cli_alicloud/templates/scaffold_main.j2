"""{{ project_name_cn }} — AgentScope 兼容入口"""

import sys


def main(query: str) -> str:
    """Echo agent：接收查询并返回占位响应

    TODO: 替换为真实的 AgentScope Agent 实现
    """
    response = (
        f"[{{ project_name_cn }}] 收到查询：{query}\n"
        "当前为占位实现，请参考 next-steps.md 完成以下工作：\n"
        "1. 接入真实数据源\n"
        "2. 实现爆文要素拆解 ReAct 循环\n"
        "3. 编写评测套件"
    )
    return response


if __name__ == "__main__":
    query = sys.argv[1] if len(sys.argv) > 1 else "默认查询"
    print(main(query))
