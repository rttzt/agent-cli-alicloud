# {{ project_name_cn }} — 评测计划

## 评测样例

| # | 输入 | 期望输出维度 |
|---|------|-------------|
{% for sample in eval_samples %}| {{ loop.index }} | {{ sample.input }} | {{ sample.expected_dimensions | join('、') }} |
{% endfor %}

## 评分维度

{% for dim in eval_dimensions %}- {{ dim }}
{% endfor %}

## 通过门槛

每个评分维度 ≥ 4/5 即通过。
