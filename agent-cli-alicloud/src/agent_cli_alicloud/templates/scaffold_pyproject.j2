[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ one_liner }}"
requires-python = ">=3.11"
dependencies = [
    "agentscope>=0.1",
    "dashscope>=1.14",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
